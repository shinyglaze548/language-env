[project]
name = "language-env"
version = "0.1.0"
description = "Language Learning RL Environment"
dependencies = [
    "fastapi",
    "uvicorn",
    "openenv>=0.2.0"
]

[project.scripts]
server = "server.app:app"
